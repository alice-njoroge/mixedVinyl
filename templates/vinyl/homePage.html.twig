
<h1> {{ title }} </h1>
<div class="div">
    Tracks:
    <ul>
        {% for key, track in tracks %}
            <li>
               {{ track.artist }} : {{ track.song}}
            </li>
        {% endfor %}
    </ul>
</div>
